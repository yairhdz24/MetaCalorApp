

export const Button = ({color, text}) => {
  return (
    <button
      // onClick={() => editarPlatillo(index)}
      className=' bg-orange-500 text-white w-1/5 rounded-md shadow-md text-sm hover:bg-orange-700 cursor-pointer p-2'
    >
      +
    </button>
  )
}
